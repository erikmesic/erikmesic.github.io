// visuals.js — editorial motion and consistent page-title treatment
(function(){
  function loadOverrides(){if(document.querySelector('link[data-phase1-overrides]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='/assets/css/phase1-overrides.css';link.setAttribute('data-phase1-overrides','true');document.head.appendChild(link);}
  function loadEditorial(){if(document.querySelector('script[data-editorial]'))return;const script=document.createElement('script');script.src='/assets/js/editorial.js';script.defer=true;script.setAttribute('data-editorial','true');document.body.appendChild(script);}
  function typeEffect(el,text,speed=58){if(window.matchMedia?.('(prefers-reduced-motion: reduce)').matches)return;el.innerHTML='';const textSpan=document.createElement('span');textSpan.className='type-text';const caret=document.createElement('span');caret.className='type-caret';el.append(textSpan,caret);let i=0;const id=setInterval(()=>{if(i<text.length)textSpan.textContent+=text[i++];else{clearInterval(id);setTimeout(()=>caret.remove(),700)}},speed);}
  function cleanProjectMeta(){document.querySelectorAll('.project-card-meta span:nth-child(2)').forEach(el=>{el.textContent=el.textContent.replace(/^featured\s*·\s*/i,'');});}
  function watchProjectCards(){cleanProjectMeta();const root=document.getElementById('featured-projects')||document.getElementById('additional-projects');if(!root)return;new MutationObserver(cleanProjectMeta).observe(root,{childList:true,subtree:true});}
  document.addEventListener('DOMContentLoaded',()=>{loadOverrides();document.querySelectorAll('.page-title').forEach(el=>{const original=el.getAttribute('data-original-text')||el.textContent.trim();el.setAttribute('data-original-text',original);typeEffect(el,original,58);});watchProjectCards();loadEditorial();});
})();
